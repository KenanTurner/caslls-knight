from bsp import *
from castle import nlp

# Things required for a class
#  Title
#  [tables]
#   subtitle (headers)
#   [rows]
#    [cols]
#    [?tests]

title = "TODO:"
def tables(doc):
    return [
        # table_24_30(doc),
        # table_30_36(doc),
        # table_36_42(doc),
        # table_42_48(doc)
    ]
def table_24_30(doc):
    def one(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def two(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def three(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def four(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def five(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def six(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def seven(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def eight(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def nine(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def ten(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    return [
        ["24-30 Months",""],
        one(doc),
        two(doc),
        three(doc),
        four(doc),
        five(doc),
        six(doc),
        seven(doc),
        eight(doc),
        nine(doc),
        ten(doc),
    ]
def table_30_36(doc):
    def one(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def two(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def three(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def four(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def five(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def six(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def seven(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def eight(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def nine(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def ten(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    return [
        ["30-36 Months",""],
        one(doc),
        two(doc),
        three(doc),
        four(doc),
        five(doc),
        six(doc),
        seven(doc),
        eight(doc),
        nine(doc),
        ten(doc),
    ]
def table_36_42(doc):
    def one(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def two(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def three(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def four(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def five(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def six(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def seven(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def eight(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def nine(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def ten(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    return [
        ["36-42 Months",""],
        one(doc),
        two(doc),
        three(doc),
        four(doc),
        five(doc),
        six(doc),
        seven(doc),
        eight(doc),
        nine(doc),
        ten(doc),
    ]
def table_42_48(doc):
    def one(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def two(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def three(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def four(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def five(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def six(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def seven(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def eight(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def nine(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    def ten(doc):
        title = "Uses "
        pattern = [isText()]
        return [title, is_pattern(pattern, doc)]
    return [
        ["42-48 Months",""],
        one(doc),
        two(doc),
        three(doc),
        four(doc),
        five(doc),
        six(doc),
        seven(doc),
        eight(doc),
        nine(doc),
        ten(doc),
    ]